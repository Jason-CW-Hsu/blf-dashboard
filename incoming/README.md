# 每月上傳區

每月新增一個 `YYYYMM` 資料夾，例如 `202607`，並在其中放入下列三份原始 PDF：

- 檔名含「勞工退休基金」
- 檔名含「勞工保險基金」
- 檔名含「國民年金保險基金」

將三份檔案提交到 `main` 後，GitHub Actions 會自動擷取資料、產生新版 Excel、重建網站並發布。

若資料表格式改變、缺少 PDF 或擷取筆數不足，更新會停止；請在 GitHub 的 Actions 頁面查看錯誤訊息。
