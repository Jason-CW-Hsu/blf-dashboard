# GitHub Pages 部署

此網站由 `site/index.html` 提供。建立組織擁有的私有 repository 後，推送至 `main` 即會由 `.github/workflows/deploy-pages.yml` 部署。

在 GitHub 的 **Settings → Pages** 將來源設為 GitHub Actions。此版本為公開網站，任何知道網址的人都能瀏覽。
